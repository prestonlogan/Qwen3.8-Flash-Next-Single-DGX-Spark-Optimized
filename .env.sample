# Copy to .env and adjust. Placeholders only — never commit .env.
# Q38_PORT=5810
# Q38_NAME=q38fn-opt
# Q38_SERVED_NAME=qwen3.8-flash-next
# Q38_HF_HOME=/path/to/.cache/huggingface
# Q38_PLE_CACHE=/path/to/.cache/vllm/ple_cache
# Q38_STATE=/path/to/state-dir            # logs + vLLM compile cache (default: <repo>/.state)
# Q38_GMU=0.786
# Q38_MAX_NUM_SEQS=4
# Q38_MAX_MODEL_LEN=262144
# Q38_NO_ADAPTER=0                         # 1 = stock MTP drafter
# HF_TOKEN=hf_xxx                          # only if your HF setup requires it; the checkpoint is public
